# === SECRETS — never commit (publishing policy: docs/L3-development/publishing-policy.md) ===
*.pfx
*.pem
*.key
*.p12
servers.json
.env
.env.*
secrets/

# === Build outputs ===
dist/
/Go/
*.exe
*.test
*.out

# === Toolchain / deps ===
node_modules/
vendor/
.task/

# === Editor / OS ===
.idea/
.vscode/
*.swp
Thumbs.db
.DS_Store

# === Scratch ===
tmp/
*.log

# coverage reports (sinh khi scan sonar)
coverage.out
**/coverage.out

# sonar-scanner work dir (chứa serverUrl nội bộ — KHÔNG commit)
.scannerwork/

# MariaDB/pwsh binary stage local (không commit binary lớn — fetch-deps copy từ đây)
installer/vendor/*
!installer/vendor/README.md

# Local run/preview artifacts (không commit)
run-local/
.claude/launch.json
